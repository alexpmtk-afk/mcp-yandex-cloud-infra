name: Test Telegram Egress via Cloudflare WARP

on:
  push:
    branches:
      - feature/telegram-news-reader
    paths:
      - ".github/workflows/telegram-reader-warp-egress-test.yml"

permissions:
  contents: read

concurrency:
  group: telegram-reader-warp-egress-test
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    environment: test
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install Yandex Cloud CLI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/yc"
          wget -q -O "$RUNNER_TEMP/yc/yc.tgz" https://storage.yandexcloud.net/yandexcloud-yc/release/yc_linux_amd64.tar.gz
          tar -xzf "$RUNNER_TEMP/yc/yc.tgz" -C "$RUNNER_TEMP/yc"
          chmod +x "$RUNNER_TEMP/yc/yc"
          echo "$RUNNER_TEMP/yc" >> "$GITHUB_PATH"

      - name: Authenticate
        shell: bash
        env:
          YC_SERVICE_ACCOUNT_KEY_JSON: ${{ secrets.YC_SERVICE_ACCOUNT_KEY_JSON }}
        run: |
          set -euo pipefail
          key="$RUNNER_TEMP/yc-key.json"
          printf '%s' "$YC_SERVICE_ACCOUNT_KEY_JSON" > "$key"
          chmod 600 "$key"
          echo "TF_VAR_yc_service_account_key_file=$key" >> "$GITHUB_ENV"
          yc config profile create telegram-warp-probe || true
          yc config set service-account-key "$key"

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Validate isolated WARP probe stack
        run: |
          set -euo pipefail
          terraform -chdir=terraform/telegram_reader_warp_test init -input=false
          terraform -chdir=terraform/telegram_reader_warp_test fmt -check
          terraform -chdir=terraform/telegram_reader_warp_test validate
          terraform -chdir=terraform/telegram_reader_warp_test plan -input=false -no-color -out=probe.tfplan

      - name: Create temporary WARP probe
        run: |
          set -euo pipefail
          terraform -chdir=terraform/telegram_reader_warp_test apply -input=false -no-color probe.tfplan

      - name: Collect serial-port result
        id: collect
        shell: bash
        run: |
          set -euo pipefail
          probe_id="$(terraform -chdir=terraform/telegram_reader_warp_test output -raw probe_id)"
          result="$RUNNER_TEMP/warp-probe.txt"
          for attempt in $(seq 1 40); do
            timeout 8s yc compute instance get-serial-port-output --id "$probe_id" --port 1 > "$result" 2>/dev/null || true
            if grep -q 'WARP_TEST_DONE' "$result"; then
              grep -E 'WARP_RESULT|WARP_TEST_' "$result" | tail -10
              exit 0
            fi
            sleep 5
          done
          echo 'WARP probe did not finish' >&2
          grep -E 'WARP_RESULT|cloud-init|FAILED|ERROR' "$result" | tail -40 || true
          exit 1

      - name: Upload WARP probe evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: telegram-reader-warp-egress-evidence
          path: ${{ runner.temp }}/warp-probe.txt
          if-no-files-found: warn
          retention-days: 7

      - name: Destroy temporary WARP probe
        if: always()
        shell: bash
        run: |
          set +e
          terraform -chdir=terraform/telegram_reader_warp_test destroy -auto-approve -input=false -no-color

      - name: Classify result
        if: steps.collect.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          result="$RUNNER_TEMP/warp-probe.txt"
          grep -q 'INSTALL=PASS' "$result" || { echo 'WARP_INSTALL=FAIL'; exit 2; }
          grep -q 'WARP=PASS' "$result" || { echo 'WARP_TUNNEL=FAIL'; exit 3; }
          if grep -q 'TG_DC2_443=PASS' "$result"; then
            echo 'TELEGRAM_WARP_EGRESS=PASS'
          else
            echo 'TELEGRAM_WARP_EGRESS=FAIL'
            exit 4
          fi

      - name: Remove provider key
        if: always()
        run: rm -f "$RUNNER_TEMP/yc-key.json"
