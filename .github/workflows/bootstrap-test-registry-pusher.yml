name: Diagnose TEST Registry Access

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/bootstrap-test-registry-pusher.yml

permissions:
  contents: read

concurrency:
  group: marketplaces-test-registry-bootstrap
  cancel-in-progress: false

jobs:
  diagnose:
    runs-on: ubuntu-latest
    environment: test
    timeout-minutes: 20
    steps:
      - name: Install Yandex Cloud CLI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/yc"
          curl -fsSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash -s -- -i "$RUNNER_TEMP/yc" -n
          echo "$RUNNER_TEMP/yc/bin" >> "$GITHUB_PATH"
          "$RUNNER_TEMP/yc/bin/yc" version

      - name: Authenticate CI service account
        shell: bash
        env:
          YC_SERVICE_ACCOUNT_KEY_JSON: ${{ secrets.YC_SERVICE_ACCOUNT_KEY_JSON }}
        run: |
          set -euo pipefail
          test -n "$YC_SERVICE_ACCOUNT_KEY_JSON"
          KEY_FILE="$RUNNER_TEMP/yc-service-account-key.json"
          printf '%s' "$YC_SERVICE_ACCOUNT_KEY_JSON" > "$KEY_FILE"
          chmod 600 "$KEY_FILE"
          CI_SA_ID="$(jq -r '.service_account_id // .serviceAccountId // empty' "$KEY_FILE")"
          test -n "$CI_SA_ID"
          echo "CI_SA_ID=$CI_SA_ID" >> "$GITHUB_ENV"
          yc config profile create github-ci || true
          yc config set service-account-key "$KEY_FILE"

      - name: Diagnose TEST registry IAM and IP policy
        shell: bash
        run: |
          set -euo pipefail
          CLOUD_ID=b1gi4pr9msenirafh2c7
          FOLDER_ID="$(timeout 45s yc resource-manager folder list --cloud-id "$CLOUD_ID" --format json | jq -r '.[]|select(.name=="marketplaces-mcp-test")|.id'|head -1)"
          test -n "$FOLDER_ID"
          yc config set folder-id "$FOLDER_ID"
          REGISTRY_ID="$(timeout 45s yc container registry list --folder-id "$FOLDER_ID" --format json | jq -r '.[]|select(.name=="marketplaces-mcp")|.id'|head -1)"
          test -n "$REGISTRY_ID"

          timeout 45s yc container registry list-access-bindings "$REGISTRY_ID" --format json > "$RUNNER_TEMP/registry-access.json"
          timeout 45s yc container registry list-ip-permissions "$REGISTRY_ID" --format json > "$RUNNER_TEMP/registry-ip.json"
          RUNNER_IP="$(curl -fsS --max-time 15 https://api.ipify.org)"
          test -n "$RUNNER_IP"

          echo "ci_service_account=$CI_SA_ID"
          echo "registry_id=$REGISTRY_ID"
          echo "runner_public_ip=$RUNNER_IP"
          echo 'registry_access_bindings:'
          jq -c '.' "$RUNNER_TEMP/registry-access.json"
          echo 'registry_ip_permissions:'
          jq -c '.' "$RUNNER_TEMP/registry-ip.json"
          echo 'registry_access_diagnostic=PASS'

      - name: Cleanup diagnostic files and credential
        if: always()
        shell: bash
        run: rm -f "$RUNNER_TEMP/yc-service-account-key.json" "$RUNNER_TEMP/registry-access.json" "$RUNNER_TEMP/registry-ip.json"
