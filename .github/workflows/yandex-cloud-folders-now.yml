name: Yandex Cloud folders now

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/yandex-cloud-folders-now.yml'

permissions:
  contents: read

jobs:
  folders:
    runs-on: ubuntu-latest
    environment: test
    timeout-minutes: 3
    env:
      YC_CLOUD_ID: b1gi4pr9msenirafh2c7
    steps:
      - name: Install YC CLI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/yc"
          curl -fsSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash -s -- -i "$RUNNER_TEMP/yc" -n
          echo "$RUNNER_TEMP/yc/bin" >> "$GITHUB_PATH"
      - name: Authenticate and list folders
        shell: bash
        env:
          YC_SERVICE_ACCOUNT_KEY_JSON: ${{ secrets.YC_SERVICE_ACCOUNT_KEY_JSON }}
        run: |
          set -euo pipefail
          printf '%s' "$YC_SERVICE_ACCOUNT_KEY_JSON" > "$RUNNER_TEMP/key.json"
          chmod 600 "$RUNNER_TEMP/key.json"
          yc config profile create folders-now || true
          yc config set service-account-key "$RUNNER_TEMP/key.json"
          yc config set cloud-id "$YC_CLOUD_ID"
          echo '=== CLOUD ==='
          yc resource-manager cloud get "$YC_CLOUD_ID" --format json --no-user-output | jq '{id,name,created_at,organization_id}'
          echo '=== FOLDERS ==='
          yc resource-manager folder list --cloud-id "$YC_CLOUD_ID" --format json --no-user-output | jq '[.[] | {id,name,status,created_at}]'
          rm -f "$RUNNER_TEMP/key.json"
