name: WB Other Cabinets Archive 2026 (DEPRECATED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow is disabled
        shell: bash
        run: |
          echo '::error::This legacy multi-cabinet workflow is disabled. It includes wb_dmitrieva and uses obsolete completed-job idempotency assumptions. Use the current REMOTE MCP refresh tools only after a read-only REMOTE archive-backend audit proves the durable backend.'
          exit 1