name: Trigger Drive OAuth Repair Once

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/trigger-drive-oauth-repair-once.yml

permissions:
  actions: write
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch repair workflow
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'repair-google-drive-oauth.yml',
              ref: 'main'
            })
            core.info('DRIVE_OAUTH_REPAIR_DISPATCHED=PASS')
