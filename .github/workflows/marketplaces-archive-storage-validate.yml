name: Marketplaces Archive Storage Validate (DEPRECATED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Yandex-era validator is disabled
        shell: bash
        run: |
          echo '::error::This validator describes the retired Yandex production archive runtime and references removed deployment files. Current Marketplaces production runs on REMOTE. Validate architecture and archive logic in alexpmtk-afk/marketplaces-mcp-ru; before any mutating archive refresh/recovery, prove the actual REMOTE durable backend from runtime/config and fail closed if it is unknown.'
          exit 1
