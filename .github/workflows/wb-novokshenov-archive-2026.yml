name: WB Novokshenov Archive 2026 (DEPRECATED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow is disabled
        shell: bash
        run: |
          echo '::error::This legacy archive workflow is disabled. Its completed-job idempotency assumptions predate refresh-cycle semantics (COMPLETE -> DISCOVER). Use the current MCP refresh tools or marketplaces-archive-refresh-targeted-acceptance.yml instead.'
          exit 1