name: Card Monitor Bridge

on:
  push:
    branches:
      - tooling/powershell-bridge-v0.1
    paths:
      - 'tools/card-monitor-bridge/command.ps1'
      - '.github/workflows/card-monitor-bridge.yml'

permissions:
  contents: read

concurrency:
  group: card-monitor-bridge
  cancel-in-progress: true

jobs:
  execute:
    runs-on: [self-hosted, Windows, X64, codex-bridge-service]
    timeout-minutes: 10
    steps:
      - name: Fetch and execute Card Monitor command
        shell: 'powershell -NoProfile -ExecutionPolicy Bypass -File "{0}"'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          Write-Host '=== CARD_MONITOR_BRIDGE_BEGIN ==='
          Write-Host "Commit: $env:GITHUB_SHA"
          Write-Host "Runner: $env:RUNNER_NAME"
          Write-Host "Computer: $env:COMPUTERNAME"
          $uri = "https://api.github.com/repos/$env:GITHUB_REPOSITORY/contents/tools/card-monitor-bridge/command.ps1?ref=$env:GITHUB_SHA"
          $headers = @{
            Authorization = "Bearer $env:GH_TOKEN"
            Accept = 'application/vnd.github+json'
            'X-GitHub-Api-Version' = '2022-11-28'
            'User-Agent' = 'card-monitor-bridge'
          }
          $resp = Invoke-RestMethod -Uri $uri -Headers $headers -Method Get
          $raw = ($resp.content -replace '\s','')
          $tmp = Join-Path $env:RUNNER_TEMP 'card-monitor-command.ps1'
          [IO.File]::WriteAllBytes($tmp, [Convert]::FromBase64String($raw))
          Write-Host '--- command.ps1 ---'
          Get-Content $tmp
          Write-Host '--- output ---'
          & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $tmp
          $exit = $LASTEXITCODE
          if ($null -eq $exit) { $exit = 0 }
          Write-Host "--- exit_code=$exit ---"
          Write-Host '=== CARD_MONITOR_BRIDGE_END ==='
          if ($exit -ne 0) { exit $exit }
