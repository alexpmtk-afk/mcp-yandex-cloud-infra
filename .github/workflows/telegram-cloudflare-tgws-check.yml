name: Telegram Cloudflare Runner Sanity
on:
  push:
    branches: [diagnostic/telegram-render-relay]
    paths: [.github/workflows/telegram-cloudflare-tgws-check.yml]
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity
        run: echo RUNNER_SANITY=PASS
