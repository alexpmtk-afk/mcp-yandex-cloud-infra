name: Test Telegram Egress via Tor SOCKS5

on:
  push:
    branches:
      - feature/telegram-news-reader
    paths:
      - ".github/workflows/telegram-reader-tor-egress-test.yml"

permissions:
  contents: read

concurrency:
  group: telegram-reader-tor-egress-test
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    environment: test
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Install Yandex Cloud CLI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/yc"
          wget -q -O "$RUNNER_TEMP/yc/yc.tgz" https://storage.yandexcloud.net/yandexcloud-yc/release/yc_linux_amd64.tar.gz
          tar -xzf "$RUNNER_TEMP/yc/yc.tgz" -C "$RUNNER_TEMP/yc"
          chmod +x "$RUNNER_TEMP/yc/yc"
          echo "$RUNNER_TEMP/yc" >> "$GITHUB_PATH"

      - name: Authenticate
        shell: bash
        env:
          YC_SERVICE_ACCOUNT_KEY_JSON: ${{ secrets.YC_SERVICE_ACCOUNT_KEY_JSON }}
        run: |
          set -euo pipefail
          key="$RUNNER_TEMP/yc-key.json"
          printf '%s' "$YC_SERVICE_ACCOUNT_KEY_JSON" > "$key"
          chmod 600 "$key"
          echo "TF_VAR_yc_service_account_key_file=$key" >> "$GITHUB_ENV"
          yc config profile create telegram-tor-probe || true
          yc config set service-account-key "$key"

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Validate isolated Tor probe stack
        run: |
          set -euo pipefail
          terraform -chdir=terraform/telegram_reader_tor_test init -input=false
          terraform -chdir=terraform/telegram_reader_tor_test fmt -check
          terraform -chdir=terraform/telegram_reader_tor_test validate
          terraform -chdir=terraform/telegram_reader_tor_test plan -input=false -no-color -out=probe.tfplan

      - name: Create temporary Tor probe
        run: |
          set -euo pipefail
          terraform -chdir=terraform/telegram_reader_tor_test apply -input=false -no-color probe.tfplan

      - name: Collect serial-port result
        id: collect
        shell: bash
        run: |
          set -euo pipefail
          probe_id="$(terraform -chdir=terraform/telegram_reader_tor_test output -raw probe_id)"
          result="$RUNNER_TEMP/tor-probe.txt"
          for attempt in $(seq 1 30); do
            timeout 8s yc compute instance get-serial-port-output --id "$probe_id" --port 1 > "$result" 2>/dev/null || true
            if grep -q 'TOR_TEST_DONE' "$result"; then
              grep -E 'TOR_RESULT|TOR_TEST_' "$result" | tail -10
              exit 0
            fi
            sleep 5
          done
          echo 'Tor probe did not finish' >&2
          grep -E 'TOR_RESULT|cloud-init|FAILED|ERROR' "$result" | tail -40 || true
          exit 1

      - name: Upload Tor probe evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: telegram-reader-tor-egress-evidence
          path: ${{ runner.temp }}/tor-probe.txt
          if-no-files-found: warn
          retention-days: 7

      - name: Destroy temporary Tor probe
        if: always()
        shell: bash
        run: |
          set +e
          terraform -chdir=terraform/telegram_reader_tor_test destroy -auto-approve -input=false -no-color

      - name: Classify result
        if: steps.collect.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          result="$RUNNER_TEMP/tor-probe.txt"
          grep -q 'INSTALL=PASS' "$result" || { echo 'TOR_INSTALL=FAIL'; exit 2; }
          grep -q 'TOR=PASS' "$result" || { echo 'TOR_SOCKS=FAIL'; exit 3; }
          if grep -q 'TG_DC2_443=PASS' "$result"; then
            echo 'TELEGRAM_TOR_EGRESS=PASS'
          else
            echo 'TELEGRAM_TOR_EGRESS=FAIL'
            exit 4
          fi

      - name: Remove provider key
        if: always()
        run: rm -f "$RUNNER_TEMP/yc-key.json"
