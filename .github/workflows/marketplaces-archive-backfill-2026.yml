name: Marketplaces Archive Backfill 2026 (DEPRECATED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow is disabled
        shell: bash
        run: |
          echo '::error::This legacy all-cabinets backfill workflow is disabled. It uses a pre-refresh-cycle response contract and seller=all. Use current MCP refresh tools or marketplaces-archive-refresh-targeted-acceptance.yml with explicitly selected sellers.'
          exit 1
