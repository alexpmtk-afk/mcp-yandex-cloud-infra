name: Marketplaces Archive Backfill 2026 (DEPRECATED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow is disabled
        shell: bash
        run: |
          echo '::error::This legacy all-cabinets backfill workflow is disabled. It uses a pre-refresh-cycle response contract and seller=all. Use the current REMOTE MCP refresh tools only after a read-only REMOTE archive-backend audit proves the durable backend.'
          exit 1