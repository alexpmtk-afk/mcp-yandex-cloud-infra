name: Telegram Relay Probe

on:
  push:
    branches: [main]
    paths: [.github/workflows/telegram-relay-probe.yml]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    environment: test
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Install Yandex Cloud CLI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/yc"
          wget -q -O "$RUNNER_TEMP/yc/yc.tgz" https://storage.yandexcloud.net/yandexcloud-yc/release/yc_linux_amd64.tar.gz
          tar -xzf "$RUNNER_TEMP/yc/yc.tgz" -C "$RUNNER_TEMP/yc"
          echo "$RUNNER_TEMP/yc" >> "$GITHUB_PATH"
      - name: Authenticate
        env:
          YC_SERVICE_ACCOUNT_KEY_JSON: ${{ secrets.YC_SERVICE_ACCOUNT_KEY_JSON }}
        shell: bash
        run: |
          set -euo pipefail
          key="$RUNNER_TEMP/key.json"
          printf '%s' "$YC_SERVICE_ACCOUNT_KEY_JSON" > "$key"
          chmod 600 "$key"
          yc config profile create tg-relay-probe || true
          yc config profile activate tg-relay-probe
          yc config set service-account-key "$key"
      - name: Start VM and read non-secret relay URL
        shell: bash
        run: |
          set -euo pipefail
          folder="b1gk4p83brasi8g811jv"
          status="$(yc compute instance get telegram-news-reader --folder-id "$folder" --format json | jq -r '.status')"
          if [ "$status" != "RUNNING" ]; then yc compute instance start telegram-news-reader --folder-id "$folder" >/dev/null; fi
          for _ in $(seq 1 24); do
            status="$(yc compute instance get telegram-news-reader --folder-id "$folder" --format json | jq -r '.status')"
            [ "$status" = "RUNNING" ] && break
            sleep 5
          done
          runner_ip="$(curl -fsS https://api.ipify.org)"
          ssh-keygen -q -t ed25519 -N '' -f "$RUNNER_TEMP/tgkey"
          old_keys="$(yc compute instance get telegram-news-reader --folder-id "$folder" --format json | jq -r '.metadata["ssh-keys"] // ""')"
          printf '%s' "$old_keys" > "$RUNNER_TEMP/oldkeys"
          { printf '%s\n' "$old_keys"; printf 'ubuntu:'; cat "$RUNNER_TEMP/tgkey.pub"; } > "$RUNNER_TEMP/newkeys"
          yc compute instance add-metadata telegram-news-reader --folder-id "$folder" --metadata-from-file ssh-keys="$RUNNER_TEMP/newkeys" >/dev/null
          yc vpc security-group update-rules enpocnvnpdu2goj9nkjn --folder-id "$folder" --add-rule "description=github-tg-relay-probe,direction=ingress,port=22,protocol=tcp,v4-cidrs=$runner_ip/32" >/dev/null
          ssh_opts=(-o StrictHostKeyChecking=no -o IdentitiesOnly=yes -o BatchMode=yes -o ConnectTimeout=20 -i "$RUNNER_TEMP/tgkey")
          for _ in $(seq 1 18); do
            if ssh "${ssh_opts[@]}" ubuntu@111.88.248.156 true >/dev/null 2>&1; then break; fi
            sleep 10
          done
          for _ in $(seq 1 18); do
            if ssh "${ssh_opts[@]}" ubuntu@111.88.248.156 'sudo docker ps --format "{{.Names}}" | grep -qx telegram-reader' >/dev/null 2>&1; then break; fi
            sleep 5
          done
          relay="$(ssh "${ssh_opts[@]}" ubuntu@111.88.248.156 'sudo docker exec telegram-reader printenv TELEGRAM_WS_RELAY_URL')"
          test -n "$relay"
          echo "RELAY_URL=$relay"
          curl -sS -o /dev/null -w 'RELAY_HTTPS_STATUS=%{http_code}\n' --max-time 15 "$relay" || true
      - name: Cleanup
        if: always()
        shell: bash
        run: |
          set +e
          folder="b1gk4p83brasi8g811jv"
          ids=$(yc vpc security-group get enpocnvnpdu2goj9nkjn --folder-id "$folder" --format json 2>/dev/null | jq -r '.rules[] | select(.description=="github-tg-relay-probe") | .id')
          for id in $ids; do yc vpc security-group update-rules enpocnvnpdu2goj9nkjn --folder-id "$folder" --delete-rule-id "$id" >/dev/null; done
          if [ -f "$RUNNER_TEMP/oldkeys" ]; then yc compute instance add-metadata telegram-news-reader --folder-id "$folder" --metadata-from-file ssh-keys="$RUNNER_TEMP/oldkeys" >/dev/null; fi
          yc compute instance stop telegram-news-reader --folder-id "$folder" >/dev/null 2>&1 || true
          rm -f "$RUNNER_TEMP/key.json" "$RUNNER_TEMP/tgkey" "$RUNNER_TEMP/tgkey.pub" "$RUNNER_TEMP/oldkeys" "$RUNNER_TEMP/newkeys"
