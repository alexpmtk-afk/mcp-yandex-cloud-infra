name: LEGACY - BIRZHA Historical Preparer OIDC Bootstrap (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Record completed bootstrap state
        shell: bash
        run: |
          cat <<'EOF'
          HISTORICAL_PREPARER_BOOTSTRAP=COMPLETED
          HISTORICAL_PREPARER_SERVICE_ACCOUNT_ID=ajeadmjrbl2i9ku929cc
          HISTORICAL_PREPARER_YDB_ROLE=ydb.editor
          HISTORICAL_PREPARER_FEDERATED_CREDENTIAL_ID=ajeuj3set5a5d1rf3e27
          TEMP_BOOTSTRAP_PERMISSIONS=REMOVED
          CANONICAL_PROOF=FUNCTIONAL_OIDC_TOKEN_EXCHANGE_AND_YDB_PREPARATION
          NOTE=This workflow is retained only as historical evidence and performs no IAM mutation.
          EOF
